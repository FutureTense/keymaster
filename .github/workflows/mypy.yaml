name: mypy check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: jpetrucciani/mypy-check@master
        with:
          path: "/home/runner/work/keymaster/keymaster/custom_components/keymaster/"
